func:
  - 'math.ceil(math.floor({Input}) * 0.5)'
  - ''
  - '({Input}.floor() * 0.5).ceil()'
  - '({Input}.floor() * 0.5).ceil()'

output_level: [0, 1, 2, 3]

input: [Input]

promotion_mask: [False, False, False]

param: null
